package net.golovach.eshop.custom_view_framework;

import javax.servlet.http.HttpSessionActivationListener;
import javax.servlet.http.HttpSessionEvent;
import java.util.Map;

/*public class SessionKiller implements Runnable {
    @Override
    public void run() {
        Map<String, CustomHttpSession> sessions = CustomHttpSessionOnServerRepository.getSessions();
        for(Map.Entry<String, CustomHttpSession> pair : sessions.entrySet()){
            pair.getValue().
        }
    }
}*/
